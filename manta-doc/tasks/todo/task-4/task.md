# task-4: `manta init` / help · version

## Goal
프로젝트 초기화와 정석 help/version 경로를 구현한다.

## Notes
- `manta init [path]` — taskDir 생성, anchor 기록, 글로벌 레지스트리 등록
- `manta --help`, `manta <cmd> --help`, (선택) `manta help`
- `manta --version`
- 구현된 명령만 노출; Use/Short/Example 충실
- AI·사람이 help만 보고 올바른 호출을 만들 수 있어야 한다
