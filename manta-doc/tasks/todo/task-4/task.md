# task-4: `manta init` / `manta help`

## Goal
프로젝트 초기화와 help registry를 구현한다.

## Notes
- `manta init [path]` — taskDir 생성, anchor 기록, 글로벌 레지스트리 등록
- help registry가 source of truth (구현된 명령만 등록)
- AI가 `manta help`로 학습 가능해야 한다
